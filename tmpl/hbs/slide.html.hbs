{{!
    Template for parari slide.
}}<!DOCTYPE html>
<!--
    This files is generated by {{pkg.name}}.
    At {{now}}
    For more detail, see {{pkg.homepage}}.
-->
<html>
<head>
<meta charset="utf-8"/>
<title>{{title}}</title>
{{#each js}}<!-- Copied from {{filename}} -->
<script>{{{content}}}</script>
{{/each}}

{{#each css}}<!-- Copied from {{filename}} -->
<style>{{{content}}}</style>
{{/each}}



</head>
<body class="pr-theme-{{theme}}" id="parari-slide-root">

{{{src}}}

</body>
<script>
parari.start('parari-slide-root', {
    vLock:false,
    hLock:true,
    layers: {
        {{#each layers}}{{name}}: {
            {{#each options}}{{@key}}:{{{this}}}{{/each}}
        }{{#if @last}}{{else}},{{/if}}{{/each}}
    }
});
</script>

</html>