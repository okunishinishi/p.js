/**
 * Brower unit test for pr.isSupported
 * Runs with Kamra.
 */

"use strict";

describe('pr.isSupported', function(){

    var pr = window.parari;

    it('pr.isSupported', function(){

    });
});