/**
 * Constants for parari.
 * @membrerof para
 * @namespace constants
 */
(function (pr) {
    "use strict";

    var u = pr.utilities;

    /**
     * @lends para.constants
     */
    var c = {
        PREFIX: 'pr'
    };

    pr.constants = c;


})(window.parari = window.parari || {});