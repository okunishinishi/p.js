/**
 * Constants for parari.
 * @membrerof parari
 * @member constants
 */
(function (pr) {
    "use strict";

    /**
     * @lends para.constants
     */
    var c = {
        PREFIX: 'pr'
    };

    pr.constants = c;


})(window.parari = window.parari || {});