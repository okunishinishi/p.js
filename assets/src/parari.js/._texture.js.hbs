/**
 * Parari texture.
 * @memberof parari
 * @augments Texture
 * @constructore {{Name}}Texture
 * @param {object} options - Optional settings.
 */
(function (pr, document) {
    "use strict";

    var u = pr.utilities,
        Texture = pr.textures.Texture;

    /** @lends {{Name}}Texture */
    function {{Name}}Texture(options) {
        var s = this;
        u.copy(options || {}, s);
    }


    {{Name}}Texture.prototype = u.copy(
        /** @lends {{Name}}Texture.prototype */
        {
            /**
             * Render texture.
             * @param {CanvasRenderingContext2D} ctx - Canvas 2d context.
             * @param {CanvasRenderingContext2D} ctx - Canvas 2d context.
             * @param {number} scrollX
             * @param {number} scrollY
             */
            render: function (ctx, scrollX, scrollY) {
                var s = this,
                    bounds = s.bounds;
                if (!bounds) {
                    return;
                }
                ctx.save();



                ctx.restore();
            }
        },
        new Texture({})
    );

    u.copy(Texture, {{Name}}Texture);
    u.copy(
        /** @lends {{Name}}Texture */
        {

        },
        {{Name}}Texture);

    pr.textures.{{Name}}Texture = {{Name}}Texture;

})(window.parari = window.parari || {}, document);