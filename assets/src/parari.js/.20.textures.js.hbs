/**
 * Parari {{member}}.
 * @memberof {{memberof}}
 * @member {{member}}
 */
(function (pr, document) {
    "use strict";

    /** @lends {{member}} */
    pr.{{member}} = {
        /** Short names for {{member}}. */
        get _textureNameMap() {
            return {{{textureNameMap}}};
        }
    };


})(window.parari = window.parari || {}, document);