/**
 * Parari layers.
 * @memberof parari
 * @member layers
 */
(function (pr, document) {
    "use strict";

    /** @lends layers */
    pr.layers = {
        /** Short names for layers. */
        get _layerNameMap() {
            return {{{layerNameMap}}};
        }
    };


})(window.parari = window.parari || {}, document);